{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "^12.x"
  },
  "dependencies": {
    "bootstrap": "^4.5.0",
    "cors": "^2.8.5",
    "esm": "^3.2.25",
    "express": "^4.17.1",
    "react": "^17.0.2",
    "react-bootstrap": "^2.0.3",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "sass": "^1.26.3",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "build": "react-scripts build",
    "dev": "react-scripts start",
    "start": "NODE_ENV=production node -r esm index.js",
    "dev:static": "NODE_ENV=development nodemon -r esm index.js",
    "heroku-postbuild": "npm install && npm run build && npm run start"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "display": "standalone",
  "main": "index.js",
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
