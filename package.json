{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "^12.x"
  },
  "dependencies": {
    "bootstrap": "^4.5.0",
    "cors": "^2.8.5",
    "esm": "^3.2.25",
    "express": "^4.17.1",
    "react": "17.0.2",
    "react-bootstrap": "^1.2.2",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1",
    "sass": "^1.26.3"
  },
  "scripts": {
    "build": "react-scripts build",
    "start": "NODE_ENV=production node -r esm index.js",
    "dev": "react-scripts start",
    "dev:build": "NODE_ENV=development nodemon -r esm index.js",
    "heroku-postbuild": "npm install && npm run build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "display": "standalone",
  "main": "index.js",
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
